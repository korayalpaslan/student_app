/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    // API_URL: "https://tci-student-app.netlify.app",
    API_URL: "http://localhost:3000",
  },
};

export default nextConfig;
