/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    // API_URL: "https://studenttrackingapp.netlify.app",
    API_URL: "http://localhost:3000",
  },
};

export default nextConfig;
